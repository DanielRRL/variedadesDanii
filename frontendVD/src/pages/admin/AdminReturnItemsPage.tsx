/** AdminReturnItemsPage — Bottle and product returns (stub). */
export default function AdminReturnItemsPage() {
  return (
    <div>
      <h1 className="font-heading font-bold text-xl text-text-primary mb-2">Devoluciones</h1>
      <p className="text-muted text-sm">— Página en construcción —</p>
    </div>
  );
}
