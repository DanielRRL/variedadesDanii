/** LoginPage — Email + password login form. */
export default function LoginPage() {
  return (
    <main className="min-h-screen bg-background flex items-center justify-center px-4">
      <div className="bg-surface rounded-[12px] shadow-card w-full max-w-sm p-6">
        <h1 className="font-heading text-2xl font-bold text-brand-pink mb-6 text-center">
          Iniciar Sesión
        </h1>
        <p className="text-muted text-sm text-center">— Página en construcción —</p>
      </div>
    </main>
  );
}
