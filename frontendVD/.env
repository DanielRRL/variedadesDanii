# Variedades DANII — Frontend environment variables
# Copy this file to .env.local for local development overrides.

# Backend API base URL — must NOT have a trailing slash.
# In Docker Compose: backend service is named 'backend' on port 3000.
# For local dev outside Docker: http://localhost:3000
VITE_API_URL=http://localhost:4000
